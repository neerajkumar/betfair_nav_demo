<div class="navbar navbar-default navbar-static-top">
  <a href="#" class="brand"><h1>Angular Client</h1></a>
</div>
<div class="container">
  <div class="row span12">
    <div class="span12">
    <%= bootstrap_flash %>

      <div class="row">
        <div class="span4" ng-repeat="entry in entries">
          {{entry.name}}
        </div>
      </div>

      <div ng-controller="betfairRootCtrl">
        <form>
          <input type="text" ng-model="newEntry.name">
        </form>


        <ul>
          <li ng-repeat="entry in entries" class="span3">
            {{entry.name}}
          </li>
        </ul>
      </div>
    </div>
  </div><!--/row-->
  <footer>
    <p>&copy; Company 2014</p>
  </footer>
</div>

